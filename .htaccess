RewriteEngine On

# Redirect www to non-www
RewriteCond %{HTTP_HOST} ^www\.obelis\.ai [NC]
RewriteRule ^(.*)$ https://obelis.ai/$1 [R=301,L]

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://obelis.ai/$1 [R=301,L]